# AWS Configuration
AWS_REGION=us-east-1
DYNAMODB_TABLE=FoursquareCheckins

# Foursquare Configuration
# Either provide FOURSQUARE_OAUTH_TOKEN here directly or let the script fetch it from AWS SSM Parameter Store (/foursquare/oauth_token)
FOURSQUARE_OAUTH_TOKEN=
